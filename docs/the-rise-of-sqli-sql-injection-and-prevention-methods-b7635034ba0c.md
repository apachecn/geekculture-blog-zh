# SQLi 的兴起:SQL 注入和防范方法

> 原文：<https://medium.com/geekculture/the-rise-of-sqli-sql-injection-and-prevention-methods-b7635034ba0c?source=collection_archive---------0----------------------->

![](img/39e38be71ff222e4ca82630e02ebc598.png)

在当今世界，安全性是 web 开发中最受关注的问题，从人的出生日期到信用卡信息，一切都是在线的。每年对网站的网络攻击迅速增加。你有没有想过网站里有多少人的信息是安全的？？？作为一名网站开发人员，我遇到过很多有安全漏洞的网站，它们可以被简单的 SQL 注入技术攻击。在今天的世界里，如果你在建立网站的时候不遵循最佳实践，安全就是一个幻想。

我是 **Venkatesh Nelli** 一名全栈开发人员，非常了解网站的安全漏洞。在这篇文章中，我将告诉你如何防止 SQL 注入在你的网站上，使他们更加安全。我想你可能想知道什么是 SQL 注入？？？它将如何影响网站。

SQL 注入将查询字符串作为输入而不是数据发送，以改变程序中的实际 SQL 查询模式，从而获得想要的结果。它是世界上许多黑客用来从数据库服务器中过滤数据的最著名和最强大的黑客技术之一。有时，这些攻击可能会像窃取数据库中的所有数据或完全破坏数据库一样严重。

有许多类型的注入技术，从简单的**基于错误的注入技术**到更高级的**盲目 SQL 注入攻击。**谈到 SQL 注入攻击的类型，主要有 3 种类型

**1。基于误差的注入技术。**

**2。基于联合的注入技术。**

**3。盲 SQL 注入技术。**

在基于错误的注入技术中，攻击者根据服务器返回的错误窃取信息。当服务器向 HTML 页面返回错误时，攻击者故意向数据库发送不同模式的 SQL 查询，如**或 1=1** 。通过分析错误，我们可以轻松获得表名和列及其数据类型和其他表信息。

基于联合的 SQL 注入技术与基于误差的注入技术工作相同，但基于基于联合的结果。攻击者使用 union 运算符发送不同的 SQL 模式**和 1= (union select sys.tables)** 。使用这些基于 union 的攻击，我们可以通过猜测表名或运行基于 union 运算符的 SQL 命令来获取数据，从而获得模式名、表数据和列数据类型以及表中的其他数据。

当服务器不返回错误消息时，攻击者就向数据库发送基于布尔和基于时间的 SQL 查询，以获得表中是否有数据，这时就使用了盲 SQL 注入攻击。这是非常费力的攻击，每次基于响应，我们只会得到单个字符，但在自动化的帮助下，它成为 SQL 注入最危险的攻击之一。使用这些攻击，我们可以获得模式名称和密码哈希，通过使用数据库中的这些密码哈希，我们可以将其与随机生成的哈希进行比较，并获得模式的密码。始终限制数据库访问权限如果数据库模式拥有所有管理权限，我们可以轻松地访问数据库服务器命令 shell 来监控连接到该网络的其他服务器。利用这些盲目的 SQL 注入攻击者可以在服务器上创建他自己的数据库并获得凭证，这样他就不再需要进行任何认证。

在构建网站时，有一些简单的方法可以通过遵循最佳实践来防止所有这些攻击。

**防止 SQL 注入:**

**1。**停止 web 应用向前端网页返回服务器生成的错误信息。

**2。**在后端框架中使用对象关系映射器，如**中的实体框架。网**。

**3。**在后端使用参数化查询和存储过程。

**4。**在数据库和服务器之间建立防火墙。远程连接数据库服务器也可以阻止其他服务器的数据泄漏。

**5。**将数据库权限仅限于所需级别可以帮助数据库不公开其他模式。

一个网站只有采取从前端到数据库服务器的所有措施，并谨慎地向数据库和服务器提供权限，才能有效地保护网站。如果安全墙中的一块砖存在漏洞，就可能导致整面墙崩溃。

在这个数字世界里，信息就是金钱，每个人都想偷你的钱，有些人穿着职业白领服装，有些人穿着嘻哈服装坐在家里。充分保护信息，没有人能够在没有适当身份验证的情况下访问它。下次当你建立一个网站时，遵循上面的最佳实践来避免 SQL 注入。