# PHP 基础—第 1 部分

> 原文：<https://medium.com/geekculture/the-basics-of-php-part-1-70b327e0f7cb?source=collection_archive---------49----------------------->

![](img/e3c1328e9582f4996b1decf13b356526.png)

Photo by [Ben](https://unsplash.com/@benofthenorth?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText) on [Unsplash](https://unsplash.com/s/photos/php?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText)

有时候我们学了一门新的编程语言，却到了实用的部分，忽略了理论的部分。我喜欢在一些文章中分享一些我对 PHP 语言的学习，这是第一部分。希望这对你有帮助！

![](img/65d3231a43f8a92e7b015829bef4b1ac.png)

# 句法

PHP 代码由符号和字符组成，称为语法。

## 标点

编程语言以两种方式使用一些标点符号:作为语言操作符，在解释时引起动作(例如:，；。).

**分号:**

分号是 PHP 代码语句的终止符。代码必须以分号结束，分号告诉解析器执行语句。当缺少分号时，解析器抛出一个**解析器错误，**，这表明有语法问题。

> 在每个语句结束时，解析器在移动到下一行之前进行编译(生成字节码并执行)。

**打开、关闭和快捷标签**

PHP 使用开始和结束标记来告诉计算机使用 PHP 解析器来解释下面的代码，直到遇到结束标记，解析器才停止。

标签是: ***<？php*** ，还是 ***<？*** *(当 short_open_tags 指令被启用或— enable-short-tags 被调用时)*。 ***<标签？php echo*** ，可用作: ***<？=*** 。这导致相同的响应。

> PHP-f file.php—*启用短标签*

如果一个文件只包含 PHP 代码，最好不要使用结束标签。如果文件包含另一种代码，那么结束标记是必需的。

> 如果在一个纯 PHP 文件中不存在结束标记，并且在结束标记之后有任何东西，比如一个空行或空格，那么这个空行或空格将被发送回一个发出请求的客户端，并且肯定会导致一个难以调试的错误。

**评论**

注释代码有助于阐明意图。PHP 支持 C、C++和 Unix shell 风格的注释。

双斜杠(//)被解释为开始注释标记，这个注释可以出现在一行的任何地方。

多行注释(/*..*/)也可用。

> 大量使用评论并不是一件好事。

## **操作员**

操作符构成了 PHP 代码的重要部分，并通知解析器要采取的行动。

**算术**

标准算术运算符:

*   +加法
*   -减法
*   *乘法
*   /分部
*   %模数*(除法的余数)*

**按位**

允许操作一个字节中的单个位。

**分配**

赋值是一种在内存中存储一个值，并用标识符变量或常量引用它的方法。

**分配**

单个等号运算符不是对等式的测试，它是一个赋值运算符。

> $ value = 123

**赋值简表(组合运算符)**

PHP 是开发者可以使用的算术运算的简写。格式为:“[算术运算符]=”。这将执行并存储结果。算术运算符可以是以下任意一种:+-*/&|^>> <<./>

**递增/递减(++)**

PHP 支持 C 风格的增量和减量操作符。操作符的位置决定了操作何时发生。将它放在前面，该值将在计算前递增 1。将其置于之后，该值将在之后递增。

## **数值比较**

PHP 支持几种不同的比较值的选项。

**相等:**双等于运算符(==)比较两个值，如果相等，则返回 true 或 false。

**不等式:**这个(！=;*“bang equals”*)测试两个值的不等式。

**值/类型等式和不等式:**三元等于(===)或二元等于(！==)，不尝试强制类型，而是基于类型和值进行比较；

**大于/小于:**

PHP 支持四种大于/小于运算符:

*   < Less than
*   >大于
*   <= Less than or equal to
*   > =大于或等于

> 重要的是要知道这个:= >，是用来创建数组的。如果你试图使用这个大于或等于，PHP 将出错。

## **字符串比较**

在 PHP 中，通过使用连接运算符(.).这将两根弦连接在一起。

**阵**

数组处理是 PHP 优于其他语言的一个领域。除了许多帮助处理数组的内置函数之外。可以使用以下运算符:

*   +联合
*   ==按值相等
*   ===相同，或在值和类型上相等
*   ！=值不相等
*   <>不相等
*   ！==值或类型不相同

**联合运算符示例:**

> 请注意，结果取决于哪个数组与哪个数组相加。在重复键的情况下，将以左边的数组为准。

数组相等和不等运算符完全按照预期运行。它们比较数组和值，并返回适当的响应。注意**等于**并不等同于**等同于**。

例如:

虽然值是相同的，但在内部，键是不同的。

网部分见！